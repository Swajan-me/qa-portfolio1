{
  "name": "qa-portfolio",
  "version": "1.0.0",
  "description": "QA Automation Suite using Playwright",
  "scripts": {
    "test": "npx playwright test",
    "test:report": "npx playwright test --reporter=html"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
